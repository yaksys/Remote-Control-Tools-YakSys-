v 6.0    -------------------------------------------------------------------

   for (int num1 = 0; buffer1.Length != num1; num1++)
   {
       text1 = text1 + buffer1[num1].ToString();
   }

v 7.0   -------------------------------------------------------------------

   for (int num1 = 0; buffer1.Length != num1; num1++)
   {
       text1 = text1 + buffer1[num1].ToString();
   }

v 8.0   -------------------------------------------------------------------

   for (int num1 = 0; buffer1.Length != num1; num1++)
   {
       int num2 = buffer1[num1] * 3;
       text1 = text1 + num2.ToString();
   }

v 9.0   -------------------------------------------------------------------

   for (int num1 = 0; buffer1.Length != num1; num1++)
   {
       int num2 = buffer1[num1] * 7;
       text1 = text1 + num2.ToString();
   }

v 1.0   -------------------------------------------------------------------

   for (int int_CycleCount = 0; byteArray_KeyHash.Length != int_CycleCount; int_CycleCount++)
   {
       string_CurrentNubmer += (byteArray_KeyHash[int_CycleCount] * 8).ToString();
   }